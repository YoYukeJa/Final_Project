package RegisterEntries;

public class GroupRegiserEntry {
    protected boolean registered;

    public GroupRegiserEntry(boolean _registered){
        registered = _registered;
    }

    public boolean isRegistered(){ return registered; }
}
